# Project Documentation

- `product/`: product requirements, product positioning, UI design specs, and the Desk-based virtual company model. `v2-*` files are current; `v1-*` files are historical first-version specs.
- `implementation/sdk/`: SDK spikes, backend implementation evidence, and completion audits.
- `implementation/deployment/`: deployment verification notes and environment-specific checks.
